import React from 'react';
import { Paper, Typography } from '@mui/material';
import ViewLayer from '../ViewLayer';

const AboutView = () => {
    return (
        <ViewLayer>
            <Paper>
                <Typography>Sobre nadie</Typography>
            </Paper>
        </ViewLayer>
    );
}

export default AboutView;